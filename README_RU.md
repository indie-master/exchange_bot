# CBR-rates-bot (ранее Exchange Bot)
CBR-rates-bot — это телеграм-бот для автоматизации расчета обмена валют по курсам Центробанка России с возможностью выбора даты публикации курса.
Этот проект включает в себя всё необходимое для быстрого развертывания на VPS.

## Возможности

- Подтягивает курсы USD, EUR и CNY с сайта ЦБ РФ за любую выбранную дату.
- Умеет переводить средства во всех доступных направлениях (RUB↔USD, RUB↔EUR, RUB↔CNY, USD↔EUR, USD↔CNY и EUR↔CNY).
- Предлагает удобное меню с кнопками «вперёд/назад» и постоянной клавиатурой, поэтому возвращаться к главному экрану можно без ручного ввода `/start`.

### Как пользоваться ботом

1. Нажмите **🏠 Меню** на клавиатуре (или используйте `/start` при первом запуске), чтобы открыть актуальные курсы.
2. Выберите **💱 Конвертация**, затем задайте исходную и целевую валюты и отправьте сумму — бот рассчитает результат и покажет дату публикации.
3. Кнопка **📅 Выбрать дату** позволяет ввести дату в формате `ДД.ММ.ГГГГ` и загрузить курсы за конкретный день.

Во всех шагах доступны кнопки «Назад», поэтому между меню можно перемещаться без выхода из чата.

Вам понадобится:

`BOT-Token вашего ТГ-бота`

## Установка и запуск
Следуйте приведённым ниже шагам, чтобы установить и запустить бота на вашем сервере.

### Шаг 1: Подготовка
Убедитесь, что ваш сервер соответствует следующим требованиям:

**Операционная система:** `Linux (Ubuntu/Debian)`

**Python:** `3.8 или выше`

**Доступ к интернету**

**Установленные пакеты:** `git, python3-pip`

Если что-то из этого не установлено, скрипт сделает это за вас.

### Шаг 2: Скачивание и установка
1. Зайдите в терминал вашего сервера.
2. Скопируйте и выполните следующий набор команд:
### Скачиваем установочный скрипт
````
curl -O https://raw.githubusercontent.com/indie-master/CBR-rates-bot/main/setup_cbr_rates_bot.sh
````

### Делаем скрипт исполняемым
````
chmod +x setup_cbr_rates_bot.sh
````

### Запускаем скрипт
````
./setup_cbr_rates_bot.sh
````

### Шаг 3: Конфигурация
Во время выполнения скрипта вас попросят ввести `BOT_TOKEN` — токен вашего Telegram-бота.
Он будет автоматически сохранён в файл `.env`.

### Шаг 4: Запуск
После завершения скрипта бот запустится автоматически.

Для проверки его работы, откройте файл логов:
````
tail -f bot.log
````
Убедитесь, что бот активен и работает корректно.

## Дополнительно
### Публикация под именем CBR-rates-bot
Если у вас уже есть клон старого `exchange_bot` и нужно перенести его в новое
репозиторий `CBR-rates-bot`, выполните следующие действия:

1. Переименуйте каталог проекта, чтобы он совпадал с новым названием:
   ```
   mv exchange_bot CBR-rates-bot
   cd CBR-rates-bot
   ```
2. Удалите старый удалённый репозиторий и добавьте новый (предварительно
   создайте пустой репозиторий `CBR-rates-bot` на GitHub):
   ```
   git remote remove origin
   git remote add origin git@github.com:<ваш-аккаунт>/CBR-rates-bot.git
   ```
3. Зафиксируйте переименование (README, скрипт установки и сервис уже обновлены)
   и отправьте ветку:
   ```
   git add -A
   git commit -m "Rename project to CBR-rates-bot"
   git push -u origin main
   ```
4. Перезапустите установочный скрипт из новой ссылки, чтобы все свежие установки
   подтягивали код именно из `CBR-rates-bot`.

Так вы сохраните историю Git и безболезненно переедете в новый репозиторий.

### Решение конфликтов зависимостей
Если при выполнении `pip install -r requirements.txt` появляется ошибка вида
```
ERROR: Cannot install -r requirements.txt (line 10) and httpx==0.28.1 because these package versions have conflicting dependencies
```
значит, что используется старая виртуальная среда (или устаревшая копия
репозитория), в которой вручную был зафиксирован `httpx==0.28.1`. Текущая
версия бота зависит от `python-telegram-bot==20.8`, который сам подтягивает
совместимый `httpx~=0.26.0`, поэтому достаточно удалить устаревший pin и
установить зависимости заново.

1. Обновите репозиторий и удалите предыдущую виртуальную среду:
   ```
   cd ~/CBR-rates-bot
   git pull
   rm -rf venv
   ```
2. Создайте окружение заново и установите сокращённый список зависимостей:
   ```
   python3 -m venv venv
   source venv/bin/activate
   pip install --upgrade pip
   pip install -r requirements.txt
   ```

После этих действий `pip` перестанет пытаться поставить несовместимую версию
`httpx`, и бот будет использовать протестированные зависимости.

### Перезапуск бота
Если вам нужно перезапустить бота, выполните следующие команды:
````
cd ~/CBR-rates-bot
source venv/bin/activate`
nohup python3 exchange_bot.py > bot.log 2>&1 &
````

### Остановка бота
Для остановки процесса найдите его ID и завершите:
````
ps aux | grep exchange_bot.py
kill <PROCESS_ID>
````

## CLI-управление через `CBR-rates`

Чтобы не запоминать все команды `systemctl`, воспользуйтесь прилагаемым
скриптом `CBR-rates`.

1. Добавьте его в `PATH` (проще всего сделать симлинк):
   ```
   sudo ln -s ~/CBR-rates-bot/CBR-rates /usr/local/bin/CBR-rates
   ```
2. Убедитесь, что у файла есть права на исполнение:
   ```
   chmod +x ~/CBR-rates-bot/CBR-rates
   ```
3. Введите в терминале `CBR-rates`. Скрипт покажет интерактивное меню со
   следующими пунктами:

   | Команда | Что делает |
   |---------|------------|
   | Status  | `systemctl status cbr_rates_bot.service` без постраничного вывода |
   | Update  | `git fetch && git pull --rebase` в каталоге `~/CBR-rates-bot` |
   | Restart | `systemctl restart cbr_rates_bot.service` |
   | Reload  | `systemctl daemon-reload`, затем `systemctl reload`
   (если reload не поддерживается, выполняется restart) |
   | Logs    | Последние 100 строк `journalctl -u cbr_rates_bot.service` с
   «хвостом» |
   | Stop    | Останавливает сервис |
   | Start   | Запускает сервис |
   | Delete  | Останавливает и отключает сервис, удаляет unit-файл и каталог
   `~/CBR-rates-bot` (с подтверждением) |

Скрипт рассчитан на установку в `~/CBR-rates-bot` и unit-файл
`cbr_rates_bot.service`. При необходимости поправьте переменные `SERVICE_NAME`
и `REPO_DIR` в начале файла.

## Настройка через Systemd
### 1. Создайте файл сервиса:
Откройте редактор и создайте новый файл:

````
sudo nano /etc/systemd/system/cbr_rates_bot.service
````

Добавьте следующую конфигурацию:
````
[Unit]
Description=CBR-rates-bot
After=network.target

[Service]
User=<ваш_пользователь>
WorkingDirectory=/<ваш_пользователь>/CBR-rates-bot
ExecStart=/<ваш_пользователь>/CBR-rates-bot/venv/bin/python3 exchange_bot.py
Restart=always
RestartSec=5
StandardOutput=journal
StandardError=journal
EnvironmentFile=/<ваш_пользователь>/CBR-rates-bot/.env

[Install]
WantedBy=multi-user.target
````

Замените `<ваш_пользователь>` на имя вашего пользователя.

### 2. Перезагрузите Systemd и активируйте сервис:

````
sudo systemctl daemon-reload
sudo systemctl enable cbr_rates_bot
````

### 3. Запустите бота:

````
sudo systemctl start cbr_rates_bot
````

### 4. Рестарт после изменений:
После изменения токена или кода перезапустите сервис:

````
sudo systemctl restart cbr_rates_bot
````

### 5. Проверка статуса:
Убедитесь, что бот работает:

````
sudo systemctl status cbr_rates_bot
````

Настройка через Systemd автоматизирует перезапуск и упрощает управление ботом.

## Проблемы и решения
Скрипт выдаёт ошибку "requirements.txt не найден":

Убедитесь, что файл `requirements.txt` добавлен в репозиторий и содержит список всех зависимостей.
Бот не запускается:

Проверьте логи `(bot.log)` для получения дополнительной информации о проблеме.

Необходимо обновить код бота:

### Выполните следующие команды для обновления:
````
cd ~/CBR-rates-bot
git pull
````
