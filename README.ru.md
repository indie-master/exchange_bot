<p align="center">
  <img src="docs/logo.svg" alt="Логотип Exchange Bot" width="120" />
</p>

<p align="center">
  <b>Exchange Bot</b>
</p>

<p align="center">
  <a href="README.md">🇬🇧 English</a> |
  <a href="README.ru.md">🇷🇺 Русский</a>
</p>

Exchange Bot — телеграм-бот, который показывает актуальные курсы и управляется через systemd. CLI `CBR-rates` позволяет контролировать сервис из любого каталога после установки.

## Требования

- Linux с установленными `python3` и `git`
- Доступ в интернет для загрузки зависимостей и курсов
- Права на создание `/usr/local/bin/CBR-rates` (запустите установщик через sudo при необходимости)

## Быстрая установка (одной командой)

Клонирование, установка и создание симлинка в один шаг:

```bash
bash <(curl -fsSL https://raw.githubusercontent.com/<OWNER>/exchange_bot/main/install.sh)
```

Замените `<OWNER>` на владельца репозитория, если ваш origin отличается. Скрипт:
- создаст/активирует виртуальное окружение в корне репозитория
- установит зависимости Python
- сделает `CBR-rates` исполняемым
- создаст ссылку в `/usr/local/bin`, чтобы команда работала из любого каталога

## Ручная установка

```bash
git clone https://github.com/<OWNER>/exchange_bot.git
cd exchange_bot
./install.sh
```

После этого сразу запустите:

```bash
CBR-rates status
```

## Использование

`CBR-rates` управляет systemd-юнитом `exchange_bot`.

```bash
CBR-rates               # интерактивное меню
CBR-rates status        # systemctl status exchange_bot
CBR-rates restart       # systemctl restart exchange_bot
CBR-rates logs          # journalctl -u exchange_bot -n 40
CBR-rates -C /path status   # разовый запуск с указанием корня
```

Порядок поиска корня: флаг `-C/--root` → переменная `EXCHANGE_BOT_ROOT` → расположение самой команды/симлинка → текущий каталог. Никаких ручных `chmod` и `ln -s` не требуется — всё делает `install.sh`.

## Конфигурация

- Запишите токены и ключи в `.env`, если бот их использует.
- Дополнительно можно прописать `EXCHANGE_BOT_ROOT=/abs/путь/до/exchange_bot` в профиль shell, чтобы зафиксировать корень.

## Диагностика

- **`CBR-rates` не найден**: убедитесь, что `/usr/local/bin` есть в `PATH`, либо повторно запустите `./install.sh` с sudo.
- **Ошибка импорта Python**: перезапустите `./install.sh`, чтобы пересоздать venv и зависимости.
- **Нет прав на systemctl**: используйте `sudo CBR-rates <команда>`, если у пользователя нет прав на управление сервисом.
